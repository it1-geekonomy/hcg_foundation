import Typography from "@/lib/Typography";

export default function PatientAidPage() {
  return (
    <section className="flex min-h-screen items-center justify-center bg-slate-100">
      <Typography variant="hero" className="font-bold">
        Patient Aid
      </Typography>
    </section>
  );
}
